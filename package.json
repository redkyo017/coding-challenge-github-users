{
  "name": "coding-challenge-github-users",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "clean": "rm -rf build && mkdir build",
    "babel-node": "babel-node --presets=latest",
    "start": "nodemon ./src/server.js --exec babel-node --presets=latest",
    "build": "npm run clean && babel ./src -d ./build/src"
  },
  "dependencies": {
    "consolidate": "^0.15.1",
    "cookie-parser": "~1.4.3",
    "debug": "~2.6.9",
    "ejs": "^2.6.1",
    "express": "~4.16.0",
    "http-errors": "~1.6.2",
    "jade": "~1.11.0",
    "morgan": "~1.9.0",
    "node-fetch": "^2.1.2"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.0",
    "babel-polyfill": "^6.26.0",
    "babel-preset-latest": "^6.24.1",
    "nodemon": "^1.17.2"
  },
  "babel": {
    "presets": [
      "latest"
    ]
  }
}
